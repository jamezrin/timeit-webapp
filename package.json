{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/typeorm/**",
      "**/typeorm",
      "**/react/**",
      "**/react"
    ]
  },
  "scripts": {
    "dev": "concurrently \"yarn run dev:*\"",
    "dev:common": "yarn workspace common dev",
    "dev:frontend": "yarn workspace frontend start",
    "dev:backend": "yarn workspace backend dev",
    "prepare": "yarn workspace common build"
  },
  "devDependencies": {
    "concurrently": "6.2.0"
  }
}
